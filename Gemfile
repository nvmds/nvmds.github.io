source "https://rubygems.org"

# Matches the exact Jekyll/plugin versions GitHub Pages builds with.
gem "github-pages", group: :jekyll_plugins

# Ruby 3+ no longer bundles these by default; jekyll serve needs them.
gem "webrick", "~> 1.8"
